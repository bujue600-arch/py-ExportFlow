# A 卡 · D6（09-18 导出域前端·通道）—— 完成日志与把关清单

## 当日产出

| 产出 | 位置 | 验收 |
|---|---|---|
| 契约二 v1.1（降级与重连并行语义） | `docs/specs/sse-events.md` | 消解 v1.0 互斥表述 |
| SSE 连接状态机 | `lib/sseConnection.ts` | 场景 1/4/5/6/7/8 + 生命周期 |
| 手写 FakeEventSource（测试基建） | `lib/FakeEventSource.ts` | 同上 |
| job_version 守卫 + 跨视图缓存精准更新 | `lib/jobVersion.ts` | 11 用例（乱序/移除/total/invalidate） |
| useJobs / useJobUpdates（React 接入） | `hooks/` | 依赖注入闭环 |

全绿：`vitest 63 passed（新增 23）/ build ✓ / gate ✓`。

**今晚的设计演进**：契约 v1.0 的"退避 1/2/5/10 封顶"与"3 次失败降级"互斥，写场景测试时暴露。解法：降级（轮询）与重连（5/10s 退避）并行，重连成功即升级——契约更新为 v1.1。"规格矛盾被测试逼出来"是规格流程健康运转的证据。

## 把关清单（今晚请过目，约 15 分钟）

1. **对照讲解 §1 的图读 `sseConnection.ts`**：能徒手画出三态迁移和每条边上的触发条件——这是 bullet ① 面试的底图。
2. **读 `applyEventToPage`**（30 行）：乱序为什么返回原引用？activeOnly 视图遇终态为什么移除而不是更新？（讲解 §3）
3. **做练习**：场景 10（降级期间重连成功自动升级）目前**没有测试**，由你亲手补上——这是真实工程里"增量开发补测试"的日常。

## 面试讲解点

1. SSE vs WebSocket 的选型论证（单向推送 + 拿回重连策略权）；
2. 版本守卫的两端分工（后端单调诚实，前端消费防御）；
3. 时序测试配方：假时钟 + 假 IO + 留痕工厂。

## 队友侧

- **C 已上线**（PR #6 环境卡已合并）——今天发 C-D6 集成卡（列表页 + 导出中心页一次接通，可分两天）；
- **B**：PR #4 的 lint 修复**仍未推送**（已拖 2 天），请再次提醒；
- 137127：仍未有任何提交。

# A 卡 · D5（09-17 导出域前端·选择）—— 完成日志与把关清单

## 当日产出

| 产出 | 位置 | 验收 |
|---|---|---|
| 契约三 v1.1（FILTER 空 filter = 全量快照） | `docs/specs/selection-payload.md` | 先于实现提交 |
| 三态选择模型纯函数 | `features/assets/lib/selection.ts` | 19 用例 |
| 导出意图与幂等键复用 | `lib/exportSubmit.ts` | 2 用例 |
| SelectionBar 受控组件 | `components/SelectionBar.tsx` | 5 组件用例 |

全绿：`vitest 40 passed / build ✓ / gate ✓`。

## 今日协作事件：第一次真实 code review

**fentang-7（B）提交 PR #4**（资产 API + 导出管线，620 行，1 commit）。我作为把关人本地验证：36 测试全过、单一入口/双形状/错误码合规，**代码批准**；仅 lint 阻塞（9 个 ruff 报错+1 行垃圾代码），已在 PR 上留 request-changes + 逐条修复指引（5 分钟工作量）。B 修复 push 后 CI 绿即可合并。

**评审带来的契约演进**：B 把 FILTER 校验从"filter 必须非空"改为"可为空对象（全量快照）"——判断其更符合前端真实流（无筛选时全选），已采纳并沉淀为契约 v1.1。这就是"实现推动规格演进"的正常路径：有 PR 记录、有评审意见、有契约版本，全程可回溯。

## 把关清单（今晚请过目，约 15 分钟）

1. **读 `selection.ts` + 对照讲解 §2**：能说出"不变量由构造保证"具体指什么（toggleRow 的分支维护）。
2. **PR #4 的评审意见**（GitHub 上可看全文）：这是你们答辩时"协作过程"的展示品——request-changes 的语气、修复指引的精确度、契约采纳的记录，都值得照着讲。
3. **练习的思考题**：为什么 no-op 要返回原引用？做完练习再对答案。

## 面试讲解点

1. 全选是谓词语义不是集合语义（为什么 ID 枚举不成立）；
2. 不变量由构造保证 vs 事后纠错；
3. 双保险（前端拦截是体验、后端拦截是安全，对象是入口不是自己人）。

## 队友侧

- **B**：修 PR #4 lint（5 分钟）→ 合并 → 做 B-D5（队列边界测试卡已出）；
- **C**：C-D5 追赶卡已出（补 D1+D2，表格加了行复选框为 D6 接选择模型做准备）——**今晚请务必在群里把 C 拉上线**；
- **137127**：角色仍未定，若归 C 组，D6 起把 C 的测试用例部分拆给他。

/** 导出中心页面（D2 骨架占位；D4 由 C 实现任务卡片列表）。 */
export default function JobCenterPage() {
  return (
    <section>
      <h1>导出中心</h1>
      <p className="empty-tip">骨架占位：D4 将接入任务列表与进度。</p>
    </section>
  );
}
